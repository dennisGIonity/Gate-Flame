```
========================================================================================
GATE^FLAME â€” ðŸ“Œ PINNED STATE / RESUME HERE
Author: Dennis Grobler (Wabakipi) | Ionity Global (Pty) Ltd | AEDI
Document ID: DOC-2026-09-016-STATE | Version: 12.0 | Updated: 2026-09-06 SAST
Governance: Policy 986 AED | License: AED 900 | CC BY-NC-SA 4.0 where stated
(c) 2018-2026 Antwerp Designs | Ionity (Pty) Ltd - All Rights Reserved - TM2
Web: https://www.ionity.today | https://www.ionity.world | Ref: https://www.ionity.co.za
Classification: PUBLIC | Building Tomorrow, Today. | Anything is Possible with God.
========================================================================================
```

> **This supersedes `gateflame-STATE-resume-here.md` (v11.0, 2026-08-31).**
> It was written as a separate file because the older doc was locked by another
> process on wabakipi and could not be rewritten safely. Read this one first;
> where the two disagree, this one wins. Fold it into the STATE doc when the
> lock clears.
## ðŸ“Œ END OF 2026-09-06 â€” START HERE

**Nothing was lost in the gap.** Nothing on disk had been touched since
2026-08-31 21:54. The canonical tree `E:\Gateflame` was clean, in sync, single
git identity, and every tag was already on the remote.

### The one thing that should worry us â€” unchanged

`~/.gateflame-signing/cert-info.txt` still contains a **keytool failure**, not a
certificate:

    Keystore was tampered with, or password was incorrect

It is now backed up in **two** places (`~/.gateflame-signing` and
`E:\Gateflame-KeystoreBackup\20260831`) â€” and both copies carry the same
failure. Two copies of a key that cannot be opened is still no key. **Do not
build a release APK until the 5-minute check in item 01 of
`docs/reports/manual-tasks-2026-08-31.html` passes.**

### âš  SSH IS DEAD ON THIS MACHINE â€” read before you push anything

No agent is running; `ssh -T git@github.com` returns `Permission denied
(publickey)`. **`origin` in `E:\Gateflame` is an SSH remote**, so `git fetch`
and `git push` through `origin` fail â€” and `fetch` fails *silently*, leaving the
remote-tracking ref stale so the branch looks "ahead 1" when it is not.

Two ways through:

1. Run `tools\load-key.cmd` (needs the passphrase â€” Dennis only), or
2. Push over HTTPS, which **works unattended right now**: Git Credential
   Manager already holds a valid token for `dennisGIonity`.

```cmd
git -C E:\Gateflame push https://github.com/dennisGIonity/Gate-Flame.git fix/mobile-dns-drops
git -C E:\Gateflame fetch https://github.com/dennisGIonity/Gate-Flame.git ^
    +refs/heads/fix/mobile-dns-drops:refs/remotes/origin/fix/mobile-dns-drops
```

The second line is not optional after an HTTPS push â€” without it the tracking
ref never moves and `git status` lies to you.

> Also cleared today: a **stale `E:\Gateflame\.git\HEAD.lock`**, 0 bytes, ~5Â½
> days old, no git process running. It silently swallowed a commit â€” the files
> staged, the commit did not happen, and the script carried on to push
> "Everything up-to-date". If a commit ever seems to vanish here, look for the
> lock first.

### Nothing lives on a single disk any more

Three archive branches pushed. No existing branch, tag or `main` was touched.

| Branch | Repo | SHA |
|---|---|---|
| `archive/dennis-working-copy-2026-08-09` | Dennis (private) | `5cddc81` â€” 3 orphan commits |
| `archive/antigravity-workspace-2026-08-11` | Dennis (private) | `949eae4` â€” 2 orphan commits |
| `archive/temp-build-scratch-2026-08-13` | Gate-Flame (public) | `a7962f5` â€” 16 files that were in no repo |

**The antigravity push was rejected first.** That history carries two vendored
JDKs â€” `.jdk/jdk-17.0.12+7/lib/modules` at 122 MB and
`.jdk21/jdk-21.0.4+7/lib/modules` at 134 MB â€” over GitHub's 100 MB hard limit.
Only the tip commit contained them, so it was rebuilt on its clean parent with
both trees removed, using a throwaway index; the working tree was never touched.
**Verified: 0 non-JDK paths differ from the original.** The unmodified `09ec6d3`
is still on local `main` in
`E:\_ARCHIVE-2026-08-16\App-antigravity-workspace`.

Skipped deliberately: the 782 MB `.gradle_user/` cache (now gitignored), and
regenerable build artifacts (`dist-kiosk/*.js`, `.gradle` binary caches).

Scanned all three trees before publishing to a public repo â€” only
`.env.example` placeholders (`GEMINI_API_KEY="MY_GEMINI_API_KEY"`), including
inside the 222 KB zip. **The real `AQ.Aâ€¦` key is still unrotated in those 14
`.env` files.**

### POPIA privacy notice â€” drafted, committed, `d8aecab`

Dennis said yes. Three artifacts, all from the same master:

- `docs/PRIVACY-NOTICE.md` â€” the notice + Annex A (Play) + Annex B (blockers)
- `docs/web/privacy.html` â€” self-contained, brand-matched, ready for
  `ionity.today/privacy`
- `docs/docx/PRIVACY-NOTICE.docx` â€” AEDI-branded review copy (gitignored)

Written against `build_payload()`, not against the design docs. Checked both
ways: no claimed field is absent from the source, and no payload key is left
undocumented. Retention (90 days / 10 000 rows) and the 900 s interval read off
`feed-receiver/gateflame_feed/config.py`.

**Two answers recorded, not left open:**

- `feeds.ionity.today` is hosted **in South Africa** â†’ POPIA s72 is not engaged,
  and `POPIA-REVIEW.md` Â§4 can be closed.
- **Information Officer: Johan Wilhelm van Antwerp, `info@ionity.today`.**

**Three findings from the drafting:**

1. **The app transmits nothing to Ionity.** No analytics/ads/crash SDKs, no
   `google-services.json`, three permissions only, and the sole
   `feeds.ionity.today` reference in `src/` is a *negative* test asserting the
   client refuses public hosts. The device identifiers leave **from the node,
   not the phone** â€” so a strict reading of Play's form would permit a "no data
   collected" badge. Annex A recommends declaring **Device or other IDs**
   anyway; that badge is not worth being the privacy company whose listing said
   "nothing" while its hardware shipped MAC addresses.
2. **The Regulator moved.** Not Braamfontein any more â€” Woodmead North Office
   Park, 54 Maxwell Drive, Woodmead, 2191, and complaints now go to
   `POPIAComplaints@inforegulator.org.za` on Form 5. Verified against
   `inforegulator.org.za` on 2026-09-06.
3. **Â§4 of the notice promises the fields are visible on the box** â€” that is the
   kiosk "what we send" screen, still unverified as built since the August
   review. Either it exists or the notice overclaims.

**Annex B â€” five things before it can be published:** company registration
number (Â§1 is a placeholder), IO registration with the Regulator, the kiosk
screen, a breach response plan, and `SECURITY.md`. Legal review still advisable,
particularly Â§12 on business customers.

### Still waiting on Dennis

| # | Action |
|---|---|
| 1 | **S10e: Settings â†’ Developer options â†’ Wireless debugging â†’ ON**, read out the IP:port. Android picks a new port every toggle. The rebuilt APK is still not installed. |
| 2 | **Keystore check** â€” manual-tasks item 01. Blocks any release build. |
| 3 | **Fleet dashboard is down** â€” `192.168.0.6:8091` refuses connection. `fleet/fleet.env.ps1` is gitignored and lives on that host, not on wabakipi. |
| 4 | Annex B items 1â€“5 above, before the notice goes live. |

Pi is up: `192.168.0.10:8080` answers.

---


```
Â© 2018â€“2026 Antwerp Designs | Ionity (Pty) Ltd â€” All Rights Reserved â€” TM2
Governance: Policy 986 AED | Building Tomorrow, Today.
```