```
========================================================================================
GATE^FLAME — 📌 PINNED STATE / RESUME HERE
Author: Dennis Grobler (Wabakipi) | Ionity Global (Pty) Ltd | AEDI
Founder: Johan Wilhelm van Antwerp | Ionity (Pty) Ltd | AEDI
Document ID: DOC-2026-09-018-STATE | Version: 13.0 | Updated: 2026-09-10 SAST
Governance: Policy 986 AED | License: AED 900 | CC BY-NC-SA 4.0 where stated
(c) 2018-2026 Antwerp Designs | Ionity (Pty) Ltd - All Rights Reserved - TM2
Web: https://www.ionity.today | https://www.ionity.world | Ref: https://www.ionity.co.za
Classification: PUBLIC | Building Tomorrow, Today. | Anything is Possible with God.
========================================================================================
```

> **Supersedes `PIN-2026-09-06.md` (v12) and `gateflame-STATE-resume-here.md` (v11).**
> Where they disagree, this one wins.

## 📌 END OF 2026-09-10 — START HERE

**Everything is pushed.** Branch `fix/mobile-dns-drops`, head `7e483cd`, six
commits today, one git identity throughout, tracking ref fetched after the
HTTPS push so `git status` is truthful. Tag `desktop-v1.0.2` pushed — that
triggers the new macOS/Linux/Windows build matrix. `main` is still behind and
still deliberately so (Dennis's merge; `git branch -f` + push, never checkout).

### What Dennis asked for today, and what happened

| Ask | Result |
|---|---|
| Remove all simulated data, actual only | **Done.** `mockData.ts` and `mockAdapter.ts` deleted. `DataSource` is `live \| offline \| connecting \| error` — no `demo`. Offline returns nulls and empty lists, renders as `—` under **NO NODE CONNECTED — NOTHING BELOW IS MEASURED**. Three store actions that wrote invented values (whitelist row, +1420 gravity, fake reboot) removed. Particle labels no longer invented. Tests inverted to assert emptiness. |
| Brand + metadata from ASSETS-IONITY-2026, add Dennis | **Done.** Tokens v1.1.0 + four marks vendored to `src/assets/brand/`; `src/config/brand.ts` names **Dennis Grobler (Wabakipi) — engineer/operator** and **Johan Wilhelm van Antwerp — founder** in their roles. Android launcher icons regenerated on-token (purple/teal/cyan on the AEDI ink field). `CITATION.cff`, README byline, all three HTML heads. |
| `.DUMP` with subfolders on the box | **Done.** `/opt/gateflame/.DUMP/{storage,profiles,ml,history,logs,exports,backups}` (`datadir.py`), created by `install.sh`/`install-automation.sh`, reported on `/system/status` (`storageHealthy`) and `/system/storage`. `state.db` deliberately does NOT move. |
| Default DNS = the box; ability to inject Cloudflare | **Done, with one honest limit.** `upstream.py`: `recursive` (default, ADR-001) \| `cloudflare` 1.1.1.1 \| `cloudflare-malware` 1.1.1.2 \| `cloudflare-family` 1.1.1.3. Applied through Pi-hole's config API, **read back, proven by a live resolution, rolled back if it does not answer**. Kiosk-scope only. **Not DoT** — Pi-hole forwards over plain 53 and the payload says `encrypted: false`. DoT needs an unbound forward-config mount into the distroless container; tracked below. |
| AI protection / small runners / ML | **Done (on-box, deterministic).** `anomaly.py`: DGA-like labels, per-client robust-z bursts, NXDOMAIN storms, new-domain spikes. Baseline in `.DUMP/ml`, history in `.DUMP/history`. Findings are "reasons to look", copy forbids "malware detected". Carries `clientAttribution: router-only \| per-device` (ADR-001). `jobs.py` + systemd timers (anomaly 5 min, selfcheck 15 min, backup 03:15). |
| Profiles, accessibility, storage of functions | **Done.** Standard/Family/Strict/Focus presets = (level, categories) through the same store methods; active profile **derived** from live settings. Accessibility (reduced motion, contrast, text scale, labels, keep-awake) stored on the box for the kiosk, locally for the phone. Kiosk **Guard** tab + phone Settings cards. |
| Startup animation | **Done.** CSS-only gate+flame splash, brand gradient, honours OS + owner reduced-motion, hands over on a timer. |
| EXE + Mac + Linux portable | **Windows built and RUN**: `release/desktop/GateFlame-Desktop-1.0.2-portable.exe`, 85,653,578 bytes, sha256 `80935094…D63B`; it discovered `GF-72TYTITQ` and showed pairing (`release/logs/desktop-shot.png`). **macOS/Linux** cannot be honestly built from this machine — `.github/workflows/release-desktop.yml` builds them on real runners; the `desktop-v1.0.2` tag has started that run. Check Actions. Unsigned on every platform. |
| Android app | **Debug APK built**: `release/GateFlame-Mobile-debug.apk`, 4,781,171 bytes, `VERSION_CODE=14 / 1.0.2`. **Release build still blocked** on the keystore check (item 01 of `manual-tasks-2026-08-31.html`). |
| Deps current, everything connected | `npm update` within semver, 0 audit findings, `pip check` clean. Pi answers `/system/status`. |

### ⚠ THE NEW AGENT IS NOT ON THE BOX YET

Probed today from the LAN: `/api/v1/profiles`, `/dns/upstream`, `/ml/anomalies`,
`/system/storage` → **404** on 192.168.0.10 (never installed); existing routes
→ 401 (installed, refusing). The kiosk Guard tab and the phone's Profile card
will say "did not answer" until the box has the new package. Two commands:

```
tools\load-key.cmd                       (passphrase - Dennis only)
bash tools/stage-pi-full.sh              (from Git-bash; needs dist-kiosk built)
# then, on the Pi:
sudo /tmp/gfstage/install.sh             (backs up, installs, compiles, restarts,
                                          READS BACK every new route, rolls back)
```

It does not touch Pi-hole's upstream, filtering or anything a device sees.

### Fixed along the way (all pinned by tests)

- feed-receiver was **422-ing every real check-in** since 2026-08-31: the agent
  sends `shield`, the strict schema forbade it. Schema now carries
  `ShieldReport` (per PRIVACY-NOTICE §5). The contract test had been red for
  ten days and nobody read it.
- `test_blocklist_readback` / `test_filtering_honesty` were red because they
  hard-coded one StevenBlack URL while `low` grew to three lists (`654dbf4`).
  They now read the wanted set from `threat_level`.
- `test_import_side_effects` was red only on Windows (bare env lacks
  `SYSTEMROOT`/`APPDATA`); fixed the harness, not the property.

Counts on wabakipi: **tsc clean · vitest 195/195 · node-agent 634/634 · feed-receiver 83/83.**

### Still open / decisions for Dennis

- 🔴 **Keystore check** (unchanged since 08-31). Blocks any release APK and Play upload.
- 🔴 **Deploy the new agent** to GF-72TYTITQ — the two commands above.
- 🟡 **DoT for the Cloudflare modes** — unbound forward-zone with
  `forward-tls-upstream: yes` mounted into `klutchell/unbound`, with restart + rollback. Not a flag flip.
- 🟡 **Code signing** for the desktop app (Windows cert, Apple Developer ID) — purchase decision.
- 🟡 Cloudflare Zero Trust Gateway (corp DoH endpoint) and Cloudflare Tunnel for the control plane were **offered and not chosen today**; both remain options.
- 🟡 Major dep bumps deliberately not taken (vite 8, vitest 5, TS 7, lucide 1.x, motion 13) — a "final" push is the wrong day for them.
- 🟡 Doctor still reports the pre-existing 19 checkouts / unpushed commits in `GateFlame-Repo` and `TempGateFlameBuild`, and a `.png` the sandbox could not delete was removed by hand — nothing new from today.
- 🟡 `docs/reports/capability-ledger-2026-08-31.html` predates today's routes; regenerate when the agent is deployed.

```
© 2018–2026 Antwerp Designs | Ionity (Pty) Ltd — All Rights Reserved — TM2
Governance: Policy 986 AED | Building Tomorrow, Today.
Anything is Possible with God.
```
