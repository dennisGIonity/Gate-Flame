```
========================================================================================
GATE^FLAME — 📌 PIN: 2026-09-21 DEEP AUDIT — RESUME HERE
Author: Dennis Grobler (Wabakipi) | Ionity Global (Pty) Ltd | AEDI
Document ID: DOC-2026-09-021-PIN | Version: 1.0 | Updated: 2026-09-24 SAST
Governance: Policy 986 AED | License: AED 900 | CC BY-NC-SA 4.0 where stated
(c) 2018-2026 Antwerp Designs | Ionity (Pty) Ltd - All Rights Reserved - TM2
Web: https://www.ionity.today | https://www.ionity.world | Ref: https://www.ionity.co.za
Classification: INTERNAL | Building Tomorrow, Today. | Anything is Possible with God.
========================================================================================
```

> The session of 2026-09-21 ran out of tokens mid-way through step 5 of 6.
> Everything below is what was actually true when it stopped. Read this, then
> `CLAUDE.md`, then carry on from **§5 — Where to pick up**.

**The ask (Dennis, verbatim intent):** deep-search the whole project, find what is
broken, fix it, suggest changes, test every capability and feature properly, with
**two screenshots per feature — one of the feature, one of its live working stats.**

---

## 1 — 🔴 THE BIG ONE: THE LAN RENUMBERED AND THE BOX IS DOWN

Measured from the workstation on 2026-09-21, not assumed:

| What | Before (every doc, `.env`, feed URL) | Now |
|---|---|---|
| Subnet | `192.168.0.0/24` | **`192.168.124.0/24`** |
| Router | TP-Link EX511 at `.0.1` | **`192.168.124.1`** — two gateway MACs seen: `34-CA-81-FE-BE-FF` (Ethernet) and `DC-65-55-64-AF-46` (Wi-Fi). New router, identity not yet read. |
| Workstation | `192.168.0.7` | **`192.168.124.2`** (Ethernet) + **`.124.3`** (Wi-Fi) |
| Pi | eth0 `.0.10`, wlan0 `.0.13` | **`192.168.124.17` / `.18`** — MACs `88-a2-9e-27-a0-9e` / `…9d`; `raspberrypi.local` → `fe80::8f32:93e6:bc13:266` |
| Fleet dashboard | `.0.3` / `.0.4` / `.0.6 :8091` | **nothing listening on :8091 anywhere on the LAN** |

**The Pi answers ping but every TCP port RESETS** — 22, 53, 8080, 8081, 80, 443 all
`ConnectionRefused` (RST, not drop). UDP 53 does not answer. Host alive, nothing
listening. Tool: `tools\tcp-verdict.ps1 <ip>`.

**Why DNS is down — found in the repo:** `dns-stack/.env` has
`GATEFLAME_LAN_IP=192.168.0.10`, written once by the installer. Docker cannot bind
port 53 to an address the box no longer holds, so Pi-hole cannot start — and every
watchdog escalation (restart, recreate, bypass) re-read the same stale `.env` and
failed identically, forever. **Fixed in code (§3.1) but NOT deployed.**

**Why sshd refuses — NOT explained by anything in the repo.** sshd binds `0.0.0.0`.
Needs eyes on the box: kiosk screen, or `systemctl status ssh gateflame-node-agent docker`.

**Also stale now:** the Pi's `GATEFLAME_FEED_URL` points at a `192.168.0.x` fleet
host that no longer exists; the workstation's manual DNS `192.168.0.10` (CLAUDE.md
"live estate") is dead — **check Wabakipi's own adapter DNS**, it may be pointing
nowhere.

### What Dennis has to do first (Claude cannot, from here)

1. Look at the Pi's screen, or plug in a keyboard and run
   `systemctl status ssh gateflame-node-agent docker --no-pager`.
2. `tools\load-key.cmd` (ssh-agent was **not running**: `ssh-add -l` → connection refused).
3. Once ssh answers, the fastest honest repair is deploying this branch's
   `dns-watchdog.sh` and letting it rewrite `.env` — or by hand, one sudo:
   `sudo sed -i 's/^GATEFLAME_LAN_IP=.*/GATEFLAME_LAN_IP=192.168.124.17/' /home/wabapi/node-agent/dns-stack/.env && cd /home/wabapi/node-agent/dns-stack && sudo docker compose up -d`
   (check which of .17/.18 is eth0 first: `ip -4 route get 1.1.1.1`).
4. Decide the new router's upstream-DNS setting per ADR-001, and a DHCP
   reservation for the Pi — this is the second time an address move has broken
   things (BUG-07).

---

## 2 — Automated suites, as run 2026-09-21

| Suite | Start of session | End of session |
|---|---|---|
| `node-agent` pytest | 633 pass / **1 fail** (`test_missing_lan_ip_degrades_loudly…`) | **649 pass** (+15 new tests) |
| `fleet` pytest | 9 pass | **12 pass** (+3 new) |
| vitest | 198 / 198 | **212 / 212** (+14 new) |
| `tsc --noEmit` | **red** — compiled `docs/archive/lost-backend/` | **green** (`tsconfig.json` now excludes docs/dist/android/…) |
| ruff | not installed on host Python; 168 style findings in sandbox, none blocking | unchanged — **not in CI** |
| Kiosk + mobile bundles | — | rebuilt, both verified production (`verify-bundle` ok) |

Run the whole gate: **`tools\run-suites.cmd`** → `tools\run-suites.last.txt`.

---

## 3 — What was fixed (all uncommitted until the pin commit; see §6)

Every behavioural fix has a test, and the two riskiest were checked for
non-vacuity by reintroducing the bug and watching the test fail.

### 3.1 Node agent — shell / stack
| File | Fix |
|---|---|
| `dns-watchdog.sh` | **`sync_lan_ip_env()`** — before every recreate / bypass / leave-bypass, compare `.env`'s `GATEFLAME_LAN_IP` with `ip -4 route get`, rewrite on mismatch, log `LAN RENUMBERED`. Also `${GATEFLAME_LAN_IP-…}` not `:-` (empty override now survives — that was the 1 failing test). Pinned: `tests/test_dns_watchdog_renumber.py` (5 tests, non-vacuity checked: 4 fail with the function stubbed out). |
| `dns-stack/docker-compose.yml`, `…bypass.yml` | Dropped the `:-192.168.0.10` default → `:?` so a missing key fails *naming the cause* instead of binding a stranger's address. |
| `install-dns-stack.sh` | `--use-locally` wrote `nameserver 1.1.1.1` as a **secondary** into the box's own resolv.conf — the forbidden pattern. Now 127.0.0.1 only. |
| `gateflame-ra-advertiser.sh` | Substring `grep -F` let `2001:db8::1` match a held `::10` → would advertise a resolver that doesn't exist. Exact field match now. |
| `gateflame-env-set.sh` | Logged `PIHOLE_PASSWORD`'s value to syslog. Redacted for PASSWORD/TOKEN/SECRET/KEY. |
| `install-automation.sh` | `jobs.env` (holds the Pi-hole password) was created world-readable, chmod'd two lines later. Created 0600 first now. |
| `deploy-on-pi.sh` | Suggested `GATEFLAME_PORT=8081` (Pi-hole's admin port) → 8090; avahi record hard-coded `8080` → `${PORT}`. |

### 3.2 Node agent — Python
| File | Fix |
|---|---|
| `upstream.py` | "Read-back" used `socket.getaddrinfo()` = `/etc/resolv.conf`, **not Pi-hole** → passed for an upstream never used. Now a real UDP query to 127.0.0.1:53 (RCODE 0 + ≥1 answer). Also removed process-global `setdefaulttimeout`. |
| `clients.py` + `main.py` | `ip neigh` failing returned `[]` = "no devices". New `list_clients_with_gap()`; both `/clients` routes now return `{clients, gap}`. |
| `main.py` `/vpn/continents` | Bare `[]` on cold cache (the 2026-08-31 Shield bug, again). Now carries `vpnGateAvailable` + `refreshing`. |
| `filtering_state.py` | Docstring claimed 3 states. Rewritten to the real contract (see §4 open question). |
| `threats.py` | Docstring still said `header sid:` — corrected to `X-FTL-SID` so nobody re-introduces BUG-18. |
| `netcheck.py` | Used `shutil.which("bash")` (the CLAUDE.md WSL trap). Probes now. |
| Pinned by | `tests/test_honesty_2026_09_21.py` (10 tests). |

### 3.3 Fleet
| File | Fix |
|---|---|
| `fleet/app.py` `node_history` | 30d/90d charts read hourly rows only → **the most recent 7 days were missing**. Unions raw window now. |
| `fleet/app.py` `_rollup_and_prune` | Mid-hour cutoff biased every boundary bucket permanently. Floored to the hour. |
| Pinned by | `fleet/test_history.py` (3 tests, non-vacuity checked: 2 fail with bugs reintroduced). |

### 3.4 Kiosk / mobile / Ionibot
| File | Fix |
|---|---|
| `kiosk/panels.tsx` | Overview knew **3 of 5** states → `degraded` showed *"Reading protection state…"* forever. Now `Record<ProtectionStatus,…>` (compiler-enforced); faults show the node's `lastError`; Overview shows `applying`; a fault no longer says *"Paused — unknown duration"*; owner's pause `reason` never shown on a fault; invented "blocks malware, phishing, C2…" copy removed; `scanned ?? 0` fabricated-zero removed; approved copy **"Your network is filtered"** + **"Malicious and unwanted domains are being blocked before your devices can reach them"**. |
| `kiosk/KioskApp.tsx` | Lock screen detail fell to telemetry gap for degraded → now `lastError`; backdrop `?? 12` invented default → `?? 0`. |
| `kiosk/kioskIonibot.ts` | Called `/system/netcheck` (doesn't exist) → `/posture/netcheck`. Every `{{gateway}}` screen was blank. |
| `mobile/HomeScreen.tsx` | "Protected" → "Your network is filtered". |
| `mobile/ControlsScreen.tsx` | "Cannot reach your box" shown for any error, incl. a node that answered. Split. Invented "more false positives" copy removed. |
| `mobile/ActivityScreen.tsx`, `MobileApp.tsx` | "Ads and trackers" claim → neutral; `?? 10` default → `?? 0`. |
| `services/apiClient.ts`, `types/api.ts` | FastAPI's `{"detail"}` was discarded → every refusal read "PUT /x failed with 409". Parsed now. |
| `ionibot/actions.ts`, `tree.ts`, `Ionibot.tsx` | Sent **no bearer token** (every control call 401'd → "check it has power"); pause sent `{minutes}` not `{duration}` (422); category disable sent wrong shape (422); `/filtering/allow` and `/pair/router/revert` **don't exist** — now say so truthfully instead of "check the power light". Three honest outcomes: unreachable / refused / not built. Tree pause options now match node durations (5m/30m/2h; "1 hour" didn't exist). |
| Pinned by | `src/components/kiosk/protectionStates.test.tsx` (14 tests, all five states). |

### 3.5 Tooling (`tools\`)
`probe-estate.ps1`, `find-box.ps1` (subnet sweep, PS 5.1-safe), `probe-pi-v6.ps1`,
`probe-box.cmd <ip>`, `tcp-verdict.ps1 <ip>` (OPEN / REFUSED / TIMEOUT — they need
opposite actions), `pi-reach.sh` + `pi-reach.cmd`, `run-suites.cmd`,
`local-console.cmd build|run` (runs the **real agent on the workstation** at
127.0.0.1:8080 with `dist-kiosk` mounted, state in `%TEMP%` — so the console can be
tested with the Pi down; it shows the honest `unconfigured` states, by design),
`git-bash.cmd <script>`.

`tools\*.last.txt` are run outputs — not committed.

**Machine trap found:** Desktop Commander's default PowerShell **strips `$`** from
inline commands. Write a `.ps1`/`.sh` into `tools\` and run the file. Same lesson as
the cmd.exe `\"` row in CLAUDE.md.

---

## 4 — Found, NOT fixed — needs Dennis or a later session

| # | Item | Why not done |
|---|---|---|
| 1 | **`protectionStatus` contract disagrees with CLAUDE.md.** Code produces `active|paused|bypass|degraded|unconfigured` (5) and `applying` is a **boolean sidecar**. CLAUDE.md says the five are `active|paused|bypass|degraded|applying`. | Code + TS type + tests now agree with each other; **CLAUDE.md's line needs correcting** — his doc, flagged not edited. |
| 2 | **Ionibot on the kiosk** — CLAUDE.md: *"mobile app only, never the kiosk"*; STATE 08-31: *"Added: Ionibot on the wall console"*. | Contradiction in his own decisions. Ask which stands. |
| 3 | `blocklists._gravity_finished` requires the domain count to *change* — a same-lists re-apply reads as `degraded`. | Needs a gravity timestamp from FTL; verify against a live box first. |
| 4 | No per-site allow route; no router-revert route. Ionibot now says so honestly. | Product decision: build or remove the actions. |
| 5 | Mobile discovery after a renumber relies on `gateflame.local` mDNS, unreliable in Android WebView. | Suggest: first-class "enter your box's address" screen + a QR on the kiosk. |
| 6 | Fleet: Basic auth + node tokens cross the LAN over plain HTTP; `list_nodes` filters in Python. | TLS via `deploy/Caddyfile.example` before it leaves the house. |
| 7 | Six docs still say the version numbers disagree — they don't (1.0.2 / code 14 everywhere). | Doc hygiene. |
| 8 | ruff (168 findings, 1 real unused import `is_loopback` in `main.py:30`) not in CI. | Low priority. |
| 9 | `dns-stack/.env` rewrite + all of §3 **not deployed to the Pi.** | Pi unreachable. |

---

## 5 — Where to pick up (in order)

1. **Run `tools\doctor.cmd`.** Confirm the pin commit (§6) is there and nothing else moved.
2. **Get the Pi back** (§1, Dennis's steps). Then `tools\probe-box.cmd 192.168.124.17`.
3. **Deploy** this branch to the Pi (`tools\stage-pi-update.sh` / `deploy-on-pi.sh` — `node-agent` on the Pi is NOT a git repo, scp only; one sudo from Dennis).
4. **Screenshots — step 5 of the original ask, not started.** Claude in Chrome was
   **not connected**; the built-in browser can't reach localhost. Either reconnect
   the extension, or point the built-in browser at the Pi once it's back
   (`http://192.168.124.17:8080/device-kiosk/`). For each feature: one shot of the
   feature, one of its live stats. List: kiosk Overview / Filtering / Threats /
   Network / Shield / Modules / WAN / System; mobile Home / Activity / Blocked /
   Shield / Network / Health / Settings; fleet dashboard + node detail + history;
   Pi-hole admin :8081.
5. **Write `docs/TEST-REPORT-2026-09-21.md`** with the shots, §2–§4 of this file,
   and the suggestions. Then push **from Git-bash** (key loaded), run doctor again.

---

## 6 — Git state at the pin

Branch `fix/mobile-dns-drops`, was **6 ahead of origin** at session start (the
2026-09-20 C-drive work, unpushed). This session's work is commit **`dfdca9d`**
(2026-09-24 09:11, `DennisIonity`), so the branch is now **7 ahead, NOT pushed** —
the SSH agent holds no key. After `tools\load-key.cmd`:

    tools\git-bash.cmd /e/Gateflame/tools/push-if-key.sh

(it refuses rather than hanging if the key still isn't loaded; result in
`tools\push-if-key.last.txt`). `tools\push-if-key.sh` and this §6 edit landed
after `dfdca9d` and are uncommitted — commit them with the next change.

Deliberately **not** staged: `tools\*.last.txt`, rebuilt `dist*/`, and the BUG-17
third-party edits (`android/version.properties`, `vite.standalone.config.ts`,
`debug.cjs`).

A stale **0-byte `.git/index.lock`** was left at 07:49 on 09-21 by a sandbox
`git checkout` that could not unlink it. The pin commit script removes it only if
no `git.exe` is running.

The local agent started by `tools\local-console.cmd run` may still be listening on
127.0.0.1:8080 — harmless; close its window or kill the `python.exe` if needed.
