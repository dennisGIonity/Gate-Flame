```
========================================================================================
GATE^FLAME — FEATURE STATUS & WHAT IS LEFT
Author: Dennis Grobler (Wabakipi) | Ionity Global (Pty) Ltd | AEDI
Founder: Johan Wilhelm van Antwerp | Ionity (Pty) Ltd | AEDI
Document ID: DOC-2026-09-013-STAT | Version: 1.0 | Updated: 2026-09-13 SAST
Governance: Policy 986 AED | License: AED 900 | CC BY-NC-SA 4.0 where stated
(c) 2018-2026 Antwerp Designs | Ionity (Pty) Ltd - All Rights Reserved - TM2
Web: https://www.ionity.today | https://www.ionity.world | Ref: https://www.ionity.co.za
Classification: INTERNAL | Building Tomorrow, Today. | Anything is Possible with God.
========================================================================================
```

**Probed live against `GF-72TYTITQ` and the fleet host on 2026-09-13.** Status is what the
box answered, not what the repo contains.

**Active** = proven doing its job. **Not active** = not running, or never proven — the
reason is on the line. Nothing is called active because the code exists.

## Two facts that set everything below

1. **The 2026-09-10 agent is still not on the box.** Probed today: `/profiles`,
   `/profiles/accessibility`, `/dns/upstream`, `/ml/anomalies`, `/system/storage` all
   return **404** (never installed). Every older route returns 401 (installed, refusing).
   So Profiles, Accessibility, Cloudflare upstream, on-box ML and `.DUMP` reporting are
   **written and pushed but not running anywhere.**
2. **The fleet dashboard is down.** No answer on `192.168.0.3:8091` or `192.168.0.6:8091/:8080`.
   It is hand-started and dies with its terminal (BUG-06).

Node reports: `{"nodeId":"GF-72TYTITQ","agentVersion":"0.1.0","provisioned":true}`

---

# 1 — MOBILE APP

| Feature | What it does | Status |
|---|---|---|
| Pairing + token | Claim a 6-digit code, store the token | **Active** — phone is paired |
| Node discovery | Finds the box on the LAN | **Active** |
| Home | Protected state, lookups, blocked, device count | **Active** — photographed showing real figures |
| Activity | Query stream over time | **Active** — pulls a live route; screen never viewed on the handset |
| Blocked (Threats) | What was blocked, top domains | **Active** — pulls a live route; screen never viewed on the handset |
| Network | Devices on the network, names | **Active** — pulls a live route; screen never viewed on the handset |
| Health | Box CPU / memory / disk / temperature | **Active** — pulls a live route; screen never viewed on the handset |
| Controls (Settings) | Threat dial, categories, pause | **Active** — pulls a live route; screen never viewed on the handset |
| Shield — country list | Pick a country per device | **Active** — VPN Gate's public server list answers |
| Shield — device rename | Name a device in the picker | **Not active** — API proven, but the button has never been pressed by a human |
| Shield — `.ovpn` handoff | Hand a config to the phone's VPN app | **Not active** — never fetched and opened end to end |
| Ionity exit servers | Our own VPN endpoints | **Not active** — they do not exist; `controlPlaneReachable: false` |
| Profiles | Standard / Family / Strict / Focus presets | **Not active** — route returns 404, the 09-10 agent is not on the box |
| Accessibility | Reduced motion, contrast, text scale | **Not active** — route returns 404, the 09-10 agent is not on the box |
| IoniBot | Guided help, question-menu | **Not active** — renders, never exercised |
| Play (Ionicrobes) | The game | **Active** — lazy-loaded, untouched since |
| Startup splash | Branded gate+flame animation | **Not active** — built, never seen on the handset |
| Debug APK | Installable build | **Active** — `VERSION_CODE=14 / 1.0.2` |
| Release APK | Signed, Play-uploadable | **Not active** — no keystore config, so it cannot be signed |

# 2 — KIOSK (on the box's own screen)

| Feature | What it does | Status |
|---|---|---|
| Served at `/device-kiosk/` | The console itself | **Active** — answered 200 today |
| Lock screen + hold-to-unlock | Physical-presence gate | **Active** |
| PIN unlock | Numeric unlock + lockout | **Not active** — no PIN is set, so the route returns 503 |
| Idle re-lock (4 min) | Locks itself when left alone | **Not active** — in the bundle, never timed on the box |
| Overview | Queries, blocked, devices, uptime | **Active** |
| Filtering | Threat dial, categories, pause | **Active** |
| Threats | Recent blocks | **Active** |
| Network | Connected devices | **Active** |
| Modules | What is running vs a named gap | **Active** |
| WAN | Link and usage | **Active** |
| System | Node identity, version, storage | **Active** |
| Firewall | Bounce a device off the network | **Not active** — route answers, but a rule has never been fired on hardware |
| Shield panel | VPN per device | **Active** for the country list; no Ionity exit servers behind it |
| Guard tab | Profiles + accessibility | **Not active** — routes return 404, the 09-10 agent is not on the box |
| Read-only for LAN viewers | A phone on `/device-kiosk` cannot act | **Active** |
| Pairing overlay | Issue a code with a countdown | **Active** — worked when last used; not re-run since |

# 3 — FLEET DASHBOARD

**The whole dashboard is off.** Each row worked on 2026-08-31; none of it is serving today,
so every row reads Not active for one reason — the process is not running.

| Feature | What it does | Status |
|---|---|---|
| Service itself | Answering at all | **Not active** — no reply on `.3:8091`, or on `.6` at either port |
| Health ingest | Boxes post in every 5 min | **Not active** — boxes are posting into nothing |
| Per-node tokens | Each box authenticates itself | **Not active** — server is down |
| Basic-auth + `/healthz` | Login wall, liveness probe | **Not active** — server is down |
| Node list + filters | Search, tag, billing | **Not active** — server is down |
| Fleet summary | Tiles, trend, counts | **Not active** — server is down |
| Node detail + history | One box over time | **Not active** — server is down |
| Admin fields + notes | Tag, billing, free text | **Not active** — server is down |
| SVG graphs | Real data; gaps break the line | **Not active** — server is down |
| Hourly rollup + 90-day retention | Keeps the database bounded | **Not active** — never observed; no data is old enough to prune |
| Remote support actions | Reach into a box and fix it | **Not active** — never built; there is no channel back to a node |
| Survives reboot | Starts on its own | **Not active** — hand-started script, dies with its terminal |

> Desktop console (Electron) is a fourth surface, not asked for: Windows portable built
> and run; macOS/Linux build on CI runners; unsigned on every platform.

---

# WHAT IS LEFT

## Blocking — do these first

| # | Item |
|---|---|
| 1 | **Deploy the 09-10 agent to the box.** `tools\load-key.cmd`, then `bash tools/stage-pi-full.sh`, then one `sudo /tmp/gfstage/install.sh` on the Pi. Unblocks five features that exist but run nowhere. |
| 2 | **Get the fleet server back up and keep it up.** Scheduled task or service, and a fixed address — it has already moved twice (`.7 → .6 → .3`). |
| 3 | **Keystore decision.** It was pasted into a chat as base64. Regenerating before the first Play upload costs nothing; after it, it costs every installed device. Then write `android/keystore.properties`. |
| 4 | **Revoke credentials.** Two GitHub PATs, `GEMINI_API_KEY`, and the AWS + OpenAI keys found yesterday. AWS first. |

## Before the Play listing

| # | Item |
|---|---|
| 5 | Signed release APK + **enrol in Play App Signing at the first upload** |
| 6 | Public privacy-notice URL (POPIA s18 needs it anyway) |
| 7 | Data-safety form — the honest answer is unusually strong, use it |
| 8 | Check the current closed-testing tester-count and duration rule |
| 9 | Screenshots, feature graphic, 512×512 store icon — none exist |

## Before it can be sold

| # | Item |
|---|---|
| 10 | **History database.** "Including yesterday's" has no implementation at all — no telemetry table, no `/history/*` route. A reboot is still amnesia. ~15 days, the largest item left. |
| 11 | **Plug and play.** Custom image, first-boot provisioning, pairing without SSH, status LEDs, OTA **with rollback**, factory reset. Today the box needs a human with SSH. |
| 12 | **Prove on hardware:** firewall bounce actually blocking, DPI capture, the `.ovpn` handoff end to end, the console PIN, 90-day retention. |
| 13 | **VPN exit servers.** Ours do not exist. Oracle Always Free and customer-boxes-as-exit-nodes were both rejected — so this needs a decision and money. |
| 14 | **Deploy the DNS watchdog / IPv6 self-heal.** Written and tested, not on the box. This is the phone-drop fix. |
| 15 | **Board decision.** BOM says Cubie A7A-6GB; the two-tier doc still says Orange Pi Zero 2W. And 6 GB reopens "can we go inline and take over DNS?" — unanswered. |
| 16 | **Pi-hole is EUPL-1.2.** If the shipped image contains it, that is redistribution. Lawyer question, filed in `THIRD_PARTY_NOTICES.md`. |
| 17 | POPIA review by a practitioner, Information Officer registered, breach plan |
| 18 | Trademark "Gate^Flame", ICASA type approval question, CPA warranty terms, per-unit serials |

## Cheap, worth clearing

| # | Item |
|---|---|
| 19 | **BUG-01** — test suite will not run from a fresh clone (`GATEFLAME_DB_PATH` undocumented) |
| 20 | **BUG-02** — `NODE_ENV=production` breaks 34 frontend tests |
| 21 | Six mobile screens changed but never looked at on the handset |
| 22 | Ruff still `continue-on-error: true` — 51 violations between here and a real gate |
| 23 | `DeploymentScriptViewer` is the last brochure screen still reachable — delete or gate it |
| 24 | Extract the lost Docker backend from the 344 MB bundle; it is one file on one machine |

```
© 2018–2026 Antwerp Designs | Ionity (Pty) Ltd — All Rights Reserved — TM2
Governance: Policy 986 AED | Building Tomorrow, Today.
Anything is Possible with God.
```
