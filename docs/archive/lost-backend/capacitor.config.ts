import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gateflame.network',
  appName: 'Gate^Flame Network Security Node',
  webDir: 'dist'
};

export default config;
